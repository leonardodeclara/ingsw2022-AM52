package it.polimi.ingsw.GUI.GUIControllers;

import javafx.fxml.FXML;
import javafx.scene.control.ProgressBar;

/**
 * Class LobbyController should contain all the logic for the Lobby Menu Scene
 * In the current version the lobby menu doesn't require player input,but that could possibly change
 * in a future update
 */
public class LobbyController extends GUIController{
    @FXML
    private ProgressBar progress;

    @FXML
    public void initialize(){
    }
}
